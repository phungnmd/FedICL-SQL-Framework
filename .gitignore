# Code lives in its own git repo (releasable standalone) — see README "Two-repo layout"
fedicl-sql/

.DS_Store
.pytest_cache/
.vscode/

# IDE / local
*.code-workspace
note.txt

# secrets — never commit
PRIVATE.txt
*.key
.env
